# mirage

# Dependencies

The following repositories are used here, and are not included as submodules so that we can 'pin' versions:

- [ImGui](https://github.com/ocornut/imgui) on `docking` branch, at commit `5e17c080`
- [ImPlot](https://github.com/epezent/implot) on `master` branch, at commit `3da8bd3`

